import {NextResponse} from "next/server";import {AuthenticationError,AuthorizationError} from "@/lib/auth/errors";import {getCurrentParticipant} from "@/lib/auth/current-participant";import {saveCurrentAssessmentModule} from "@/lib/services/participant/participant-assessment-service";
export async function PATCH(request:Request,{params}:{params:Promise<{moduleKey:string}>}){try{const participant=await getCurrentParticipant();if(participant.lifecycle_status!=="active")return NextResponse.json({success:false,message:"Assessment access is unavailable for the current participant status."},{status:403});let body:unknown;try{body=await request.json();}catch{return NextResponse.json({success:false,formError:"Invalid assessment request."},{status:400});}const {moduleKey}=await params;const result=await saveCurrentAssessmentModule(moduleKey,body);return NextResponse.json(result,{status:result.success?200:400,headers:{"Cache-Control":"private, no-store"}});}catch(error){if(error instanceof AuthenticationError)return NextResponse.json({success:false,message:"Authentication is required."},{status:401});if(error instanceof AuthorizationError)return NextResponse.json({success:false,message:"Participant assessment is unavailable."},{status:404});console.error("[WPAG Participant Assessment API] Module save failed.");return NextResponse.json({success:false,message:"The module could not be saved."},{status:500});}}
