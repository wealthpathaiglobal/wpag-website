import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
const read=(path:string)=>readFileSync(resolve(process.cwd(),path),"utf8");const dashboard=read("src/app/participant/assessment/page.tsx");const review=read("src/app/participant/assessment/review-submit/page.tsx");const modules=["financial-profile","cash-flow","debt-obligations","stability-margin","protection-risk","goals-planning"].map(name=>read(`src/app/participant/assessment/${name}/page.tsx`));
describe("durable assessment UI contracts",()=>{it("shows start, durable progress, and submitted states",()=>{expect(dashboard).toContain("Start Assessment");expect(dashboard).toContain("module_progress");expect(dashboard).toContain("submitted_at");});it("wires every module to durable persistence",()=>{for(const source of modules){expect(source).toContain("useAssessmentModule");expect(source).toContain("persistence.save");expect(source).toContain("persistence.submitted");expect(source).not.toContain("localStorage");expect(source).not.toContain("sessionStorage");}});it("reviews grouped durable answers and submits once",()=>{expect(review).toContain("assessment.answers");expect(review).toContain("/api/participant/assessment/submit");expect(review).toContain("lock.current");});it("makes no scoring diagnosis or report availability claim",()=>{expect(review).toContain("no HFOS score, diagnosis, recommendation, or report is generated");});});
